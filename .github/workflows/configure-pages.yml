name: Configure GitHub Pages

on:
  push:
    branches: [main]

jobs:
  configure:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          branch: main
          path: /
          enablement: true
